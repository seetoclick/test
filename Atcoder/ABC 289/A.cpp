#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int t, n;

void solve() {
	string s;
	cin >> s;
	string r;
	for (auto c : s) {
		if (c == '0')
			cout << 1;
		else
			cout << 0;
	}
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	solve();		
	
	return 0;
}
