#include <bits/stdc++.h>
using namespace std;
using LL = long long;

int m, s, t;
LL dp[100009][1009];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	cin >> m >> s >> t;
	for (int i = 1; i <= t; ++i) {
		for (int j = 0; j <= m; ++j) {
			int Go = 17;
			if ()
		}
	}
}
