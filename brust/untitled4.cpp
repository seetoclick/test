#include <bits/stdc++.h>
using namespace std;
using LL = long long;

const int N = 1000;
int n, k;
LL C[N + 5][N + 5], D[N + 5], dp[N + 5];

void solve() {
	cin >> n;
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	solve();
	
	return 0;
}
