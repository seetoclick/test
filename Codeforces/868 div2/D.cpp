#include <bits/stdc++.h>
using namespace std;
using LL = long long;

LL t, n, k;
const LL P = 1e9 + 7;


void solve() {
	LL x[35], c[35];
	cin >> n >> k;
	for (LL i = 1; i <= k; ++i) {
		cin >> x[i] >> c[i];
	}
	vector<char> s(n + 1, 0);
	for (int i = 1; i <= k; ++i) {
		
	}
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	cin >> t;
	while (t--) {
		solve();
	}
	
	return 0;
}
